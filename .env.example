# No environment variables are required today — authentication is mocked.
# These will be needed once real Auth0 is wired in (see README "Authentication" section):
# NEXT_PUBLIC_AUTH0_DOMAIN=
# NEXT_PUBLIC_AUTH0_CLIENT_ID=
# NEXT_PUBLIC_AUTH0_REDIRECT_URI=
